{
  "name": "obsidian-publish-to-telegram",
  "version": "2.7.0",
  "description": "Allows you to post notes directly to Telegram channels and groups with different presets.",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js --watch",
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js --minify"
  },
  "keywords": [],
  "author": "pan4ratte",
  "license": "AGPL-3.0",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "obsidian": "latest",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "telegram-markdown-v2": "^0.0.4"
  }
}
